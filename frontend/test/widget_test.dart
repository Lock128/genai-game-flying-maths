import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flying_maths/main.dart';

void main() {
  testWidgets('Flying Maths has a title and start button', (WidgetTester tester) async {
    //await tester.pumpWidget(const MyApp());

    //expect(find.text('Flying Maths'), findsOneWidget);
    //expect(find.text('Start Game'), findsOneWidget);
  });

  testWidgets('Difficulty selection works', (WidgetTester tester) async {
    //await tester.pumpWidget(const MyApp());

   // expect(find.text('medium'), findsOneWidget);

    //await tester.tap(find.byType(DropdownButton<String>));
    //await tester.pumpAndSettle();

    //await tester.tap(find.text('hard').last);
    //await tester.pumpAndSettle();

    //expect(find.text('hard'), findsOneWidget);
  });
}